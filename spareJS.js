
  // const hatchLoseGeese = (gooseArr) => {
  //   console.log(timesRun)
  //   // console.log(gooseArr)
  //   gooseArr[timesRun].style.display = "inline-block" // not changing display from none to inline
  //   despawnGoose = setInterval(()=>{
  //     gooseArr[timesRun].remove()
  //   },8000)
  //   timesRun+=1;
  //   console.log(gooseArr.length + " " + timesRun)
  //   if (timesRun===gooseArr.length){
  //     clearInterval(gooseInterval)
  //     clearInterval(despawnGoose)
  //   }
  //   // hatchLoseGeese(gooseArr)
  // };

  // gooseInterval = setInterval(hatchLoseGeese(gooseArr), 5000);
  // setinterval(function, time) function will happen every time seconds
  // clearinterval(intervalID) in function using counter and if check. or while

  // write a function that hatches a goose from the array every x seconds then uses settimeout to clear it from display
  // Set timeout(function, time) function will happen after given time